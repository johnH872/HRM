import { HandleDisplayColorPipe } from './handle-display-color.pipe';

describe('HandleDisplayColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HandleDisplayColorPipe();
    expect(pipe).toBeTruthy();
  });
});
