import { CheckColorTodayPipe } from './check-color-today.pipe';

describe('CheckColorTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckColorTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
