import { HighlightTextSearchingPipe } from './highlight-text-searching.pipe';

describe('HighlightTextSearchingPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightTextSearchingPipe();
    expect(pipe).toBeTruthy();
  });
});
