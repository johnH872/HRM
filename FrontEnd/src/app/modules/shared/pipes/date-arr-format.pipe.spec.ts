import { DateArrFormatPipe } from './date-arr-format.pipe';

describe('DateArrFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateArrFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
